export enum ItemTypes {
  COLUMN = 'column',
}

export enum SortType {
  ASC = 'asc',
  DESC = 'desc',
}

export enum Direction {
  LEFT = 'left',
  RIGHT = 'right',
}

export enum FilterType {
  STRING = 'string'
}
